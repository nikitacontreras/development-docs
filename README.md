#Development-docs
This documentation uses mkdocs to run (Python3)
To install mkdocs you must use:

	python3 -m pip install mkdocs
	#or
	pip install mkdocs

To run use :

	python -m mkdocs serve
	#or
	mkdocs serve //Only if python3 packages app is on PATH environment